{
  "name": "tollbooth-fermyon-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "HTTPS->Nostr relay fetch bridge for the tollbooth-fermyon Wasm operator (PoC plumbing).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
